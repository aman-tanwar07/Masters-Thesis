# Outputs

Figures produced by the analysis notebooks are written to Outputs/Figures/ when the notebooks are run. That directory is excluded from version control, so it will be empty in a fresh clone.

The figures themselves are not lost: notebook outputs are committed, so every plot is visible inline in the notebook that produced it.
