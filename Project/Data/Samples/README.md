# Validation sample

The sample of 199 articles was drawn by stratified random sampling, stratified
jointly by source and year bin, with the seed fixed at 42. It was locked before
any system was run, so no system's performance could influence which articles it
was evaluated on.

Article identifiers and sampling metadata are recorded here; the article texts
themselves are not committed (see `Project/Data/Raw/README.md`).
